---
description: List all synthetic-data projects grouped by folder as a markdown table
---

Call the `list_groups` and `list_projects` MCP tools on the synthetic-data MCP server.

Render a markdown table with columns: Folder | Project | Tables | Updated. Sort by folder name (Uncategorized last), then by project name. Use ISO date format (YYYY-MM-DD).

Output ONLY the table — no preamble.
